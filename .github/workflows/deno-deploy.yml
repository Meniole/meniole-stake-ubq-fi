name: Deploy (Deno Deploy)

on:
  push:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      id-token: write
    env:
      PROJECT: stake-ubq-fi
      PREVIEW_PROJECT: stake-ubq-fi-preview
      PROD_BRANCH: ${{ github.event.repository.default_branch }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: 2.x

      - name: Install Bun
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "${HOME}/.bun/bin" >> "$GITHUB_PATH"

      - name: Install deployctl
        run: |
          deno install -gArf jsr:@deno/deployctl@1.12.0
          echo "${HOME}/.deno/bin" >> "$GITHUB_PATH"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      - name: Deploy
        env:
          DENO_DEPLOY_TOKEN: ${{ secrets.DENO_DEPLOY_TOKEN }}
        run: |
          set -euo pipefail
          target="$PREVIEW_PROJECT"
          prod_flag=""
          if [ "${GITHUB_REF_NAME}" = "$PROD_BRANCH" ]; then
            target="$PROJECT"
            prod_flag="--prod"
          fi

          # Derive excludes from .gitignore, but keep built assets in dist/.
          mapfile -t ignored_paths < <(git ls-files -o --ignored --exclude-standard --directory --no-empty-directory)
          excludes=()
          for path in "${ignored_paths[@]}"; do
            case "$path" in
              dist|dist/*) continue ;;
            esac
            excludes+=("--exclude=${path}")
          done

          deployctl deploy \
            --project="$target" \
            $prod_flag \
            --entrypoint=serve.ts \
            --root="." \
            --token="$DENO_DEPLOY_TOKEN" \
            --include="." \
            --include="deno.json" \
            "${excludes[@]}"
